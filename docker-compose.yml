version: '3.8'

services:
  cloudspace:
    build: .
    container_name: cloudspace_app
    ports:
      - "8080:8080"
    environment:
      - DATA_DIR=/app/data
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: unless-stopped
